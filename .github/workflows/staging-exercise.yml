name: Staging exercise

on:
  workflow_dispatch:
  schedule:
    - cron: "40 2 * * 1"

permissions:
  contents: read

jobs:
  staging:
    runs-on: ubuntu-latest
    services:
      postgis:
        image: postgis/postgis:16-3.4
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
          POSTGRES_DB: ictmap
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: "postgresql://postgres:postgres@localhost:5432/ictmap?schema=public"
      DIRECT_URL: "postgresql://postgres:postgres@localhost:5432/ictmap?schema=public"
      NEXTAUTH_SECRET: "ci-test-secret-not-for-production-use-32ch"
      NEXTAUTH_URL: "http://127.0.0.1:3000"
      CAPTCHA_DISABLED: "1"
      BACKUP_ENCRYPTION_KEY: "ci-backup-encryption-key-min-16"
      ALLOW_DEMO_USERS: "0"
      SEED_ADMIN_PASSWORD: "CI-Temp-Admin-Pass-99!"
      ALLOW_DATABASE_RESET: "1"
      GEOCODER_DISABLED: "1"
      HIBP_DISABLED: "1"
      E2E: "1"
      RATE_LIMIT_ALLOW_MEMORY: "1"
      TRUST_PROXY: "0"
      KEEP_RESTORE_DB: "1"
      REMOTE_STAGING_BASE_URL: ${{ secrets.STAGING_BASE_URL }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - name: PostgreSQL client
        run: sudo apt-get update -qq && sudo apt-get install -y -qq postgresql-client
      - name: Full staging exercise
        run: npm run staging:exercise
      - name: Build and start app against restored database
        run: |
          if [ -n "$REMOTE_STAGING_BASE_URL" ]; then
            echo "STAGING_BASE_URL=$REMOTE_STAGING_BASE_URL" >> "$GITHUB_ENV"
            echo "Using remote staging app"
            exit 0
          fi
          RESTORE_URL=$(node -e 'const fs=require("fs"); const r=JSON.parse(fs.readFileSync("data/dr-restore.json","utf8")); process.stdout.write(r.restoreUrl)')
          echo "DATABASE_URL=$RESTORE_URL" >> "$GITHUB_ENV"
          echo "DIRECT_URL=$RESTORE_URL" >> "$GITHUB_ENV"
          npm run build
          DATABASE_URL="$RESTORE_URL" DIRECT_URL="$RESTORE_URL" npx next start -p 3000 &
          echo $! > .staging-app.pid
          for i in $(seq 1 30); do
            if curl -fsS http://127.0.0.1:3000/api/health/live >/dev/null; then
              echo "STAGING_BASE_URL=http://127.0.0.1:3000" >> "$GITHUB_ENV"
              exit 0
            fi
            sleep 2
          done
          echo "staging app did not become ready against restored DB"
          exit 1
      - name: Smoke restored origin
        env:
          PRODUCTION_APP_URL: ${{ env.STAGING_BASE_URL }}
        run: node scripts/post-deploy-verify.js
      - uses: grafana/setup-k6-action@v1
        continue-on-error: true
      - name: National load suite
        env:
          BASE_URL: ${{ env.STAGING_BASE_URL }}
          LOAD_PROFILE: national
        run: |
          if command -v k6 >/dev/null; then
            k6 run scripts/performance/k6-national.js
          else
            npm run load:national
          fi
      - name: Stop local staging app
        if: always()
        run: |
          if [ -f .staging-app.pid ]; then kill "$(cat .staging-app.pid)" || true; fi
